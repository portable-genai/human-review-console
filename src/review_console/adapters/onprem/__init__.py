"""On-prem profile: fail-fast portability placeholders (the sovereign-exit proof, P-12).

Each adapter satisfies its port Protocol but refuses at call time, so the client wires their own
store / audit sink / IdP. See ``docs/onprem-migration.md``.
"""
