"""Pure-stdlib domain core: the maker-checker decision, kernel types, and artifact models.

Nothing here imports a web framework or a cloud SDK. The commons packages it uses
(``hex-service-kit`` enums, ``pii-kit``) are themselves pure standard library.
"""
